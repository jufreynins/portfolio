---
interface Props {
  as?: string;
  type?: 'fade-up' | 'stagger' | 'scale' | 'mask';
  class?: string;
}

const { as: Tag = 'div', type = 'fade-up', class: className = '' } = Astro.props;
---

<Tag class={className} data-reveal data-reveal-type={type}>
  <slot />
</Tag>
