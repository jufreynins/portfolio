---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className = '', ...rest } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-6xl px-5 sm:px-8', className]} {...rest}>
  <slot />
</div>
