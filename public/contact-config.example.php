<?php
// Copy this file to contact-config.php (same directory) and fill in the real secret key.
// contact-config.php is gitignored on purpose — never commit the real reCAPTCHA secret key.
// On the live server, contact-config.php must be uploaded directly (File Manager/FTP) since
// git deploys never push it.

$recaptchaSecretKey = 'your-recaptcha-v3-secret-key-here';
